---
name: note_writing
when_to_use: Writing the final desk note from computed facts.
examples:
  - "Executive summary first: net move, dominant driver, main offset."
---

# Writing the desk note

- TOP-DOWN: the executive summary comes first and answers the question in 2-3
  sentences — net move, dominant driver + main offset, the verdict
  (concentrated/offsetting/broad). Senior readers must not hunt for the
  bottom line.
- Then one `## section` per blueprint section, in order: 1-3 sentences each,
  INTERPRETING the artifacts (charts/tables render under your text — never
  re-list rows the table already shows).
- Numbers: reported units, thousands separators, sign conventions said in
  words ("less negative", "an offset"); percentages of the NET move.
- Every claim traces to a computed artifact. Structural insight that IS in the
  data (new-position build, paired offsetting legs, one-name concentration)
  must be stated. Causal market stories that are NOT in the data must not.
  Labelled context is allowed: "context: ...".
- State the data window once (COB dates used) — typically in the summary or a
  final context line.
- If a section's data was genuinely unavailable, one honest sentence about
  what is missing beats a paragraph of hedging.
